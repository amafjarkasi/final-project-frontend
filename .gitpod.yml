ports:
    - port: 3000
      onOpen: open-browser
tasks:
    - init: >
        npm i vercel -g &&
        npm install &&
        cp .env.example .env
      command: >
        npm i vercel -g

vscode:
  extensions:
    - dbaeumer.vscode-eslint@2.1.8:02aHhbJ0Q4aGdjHXlTdVKg==